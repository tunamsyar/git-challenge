"cp" functions as to copy files and directories:

1. To copy files:
 -cp file1 file2
  *this command would copy file1 and create file2
  *if file2 exists, it would overwrite the contents of file2 with contents of file1
  
2. To copy directories:
 -cp file1 directory1
  *this command would copy contents of file1 to directory1
